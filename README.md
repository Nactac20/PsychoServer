# PsychoServer — Сервис по работе с психологами

## Описание проекта

Серверная часть сервиса для записи к психологам. Реализована бизнес-логика с многопоточной обработкой данных и хранением в SQLite.

Проект выполнен в рамках дисциплины "Многопоточное программирование". 

---

# Этап 1: Разработка бизнес-логики сервера.

## Функциональность

### Пользователи
- Регистрация клиентов и психологов
- Поиск пользователей по email и ID
- Валидация паролей
- Защита от дублирования email

### Расписание
- Добавление слотов для записи
- Просмотр свободных слотов (только будущие, не забронированные)
- Автоматическое исключение просроченных слотов

### Бронирование
- Потокобезопасное бронирование слотов
- Защита от двойной записи (транзакции + мьютексы)
- Создание сеанса после успешного бронирования
- Проверка существования клиента и психолога
- Запрет бронирования просроченных слотов

### Логирование
- Многопоточный логгер с записью в файл и консоль
- Уровни: INFO, WARNING, ERROR
- Временные метки для каждого сообщения

---

## Технологии

- **C++17**
- **SQLite3** — хранение данных
- **GoogleTest** — unit-тестирование
- **CMake** — сборка проекта
- **STL** (многопоточность, chrono, regex)

---

## Структура проекта
```
PsychoServer/
├── external/ # Внешние зависимости (SQLite)
├── src/
│ ├── utils/
│ │ └── Logger.h # Многопоточный логгер
│ ├── models/
│ │ ├── UserRole.h # Роли пользователей
│ │ ├── User.h # Модель пользователя
│ │ ├── Slot.h # Модель слота
│ │ └── Session.h # Модель сеанса
│ └── database/
│ ├── Database.h # Интерфейс БД
│ └── Database.cpp # Реализация БД
├── tests/
│ ├── CMakeLists.txt
│ └── test_database.cpp # Тесты
├── CMakeLists.txt # Корневой файл сборки
├── main.cpp # Демо-программа
└── README.md
```
---

## Сборка и запуск

### Требования
- CMake 3.16+
- Компилятор с поддержкой C++17 (MSVC, GCC, Clang)
- SQLite3 (автоматически подключается)

### Сборка

```bash
git clone https://github.com/Nactac20/PsychoServer.git
cd PsychoServer
mkdir build
cd build
cmake ..
cmake --build . --config Release

# Запуск демо:
./Release/psycho_demo.exe          # Windows
./bin/psycho_demo                  # Linux/macOS

# Запуск тестов:
./tests/Release/psycho_tests.exe   # Windows
./tests/psycho_tests                # Linux/macOS

```

---

## Многопоточность и безопасность
- Мьютексы (std::recursive_mutex) — защита общих данных
- Транзакции SQLite (BEGIN IMMEDIATE) — атомарность операций
- Проверка sqlite3_changes() — контроль обновлений
- Тест с 10 потоками — доказательство корректности

---

## Автор
### Круглова Анастасия, студент 3-его курса ИС-2027
---